# Triage labels

Use these canonical labels:

- `needs-triage` — maintainer needs to evaluate the item.
- `needs-info` — waiting for information from the reporter.
- `ready-for-agent` — fully specified and ready for an agent to implement.
- `ready-for-human` — requires a human decision or implementation.
- `wontfix` — will not be actioned.
