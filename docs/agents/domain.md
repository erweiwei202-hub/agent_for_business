# Domain documentation

This is a single-context project.

- The project glossary and stable domain vocabulary belong in `CONTEXT.md`.
- Architectural decisions belong in `docs/adr/`.
- Read `CONTEXT.md` before changing domain behavior once it exists.
- Read relevant ADRs before changing the environment adapter, reward contract, data schema, SFT pipeline, or GRPO loop.
